rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write so the app can initialize the key if missing
    match /secrets/gemini {
      allow read, write: if true;
    }
  }
}